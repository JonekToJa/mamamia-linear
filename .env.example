# Local dev: point at a Postgres instance (Docker, Railway-linked, etc.)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/mamamia?schema=public

# UUID of the seeded "current user". After running `npm run db:seed`, copy the
# printed id here. If unset, lib/currentUser.ts falls back to the first user.
MOCK_USER_ID=
